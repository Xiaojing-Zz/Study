
- [x] 完成任务一
- [ ] 完成任务二
- [ ] 完成任务三

**(5).完整的查询语句**
```
SELECT 列名, 聚合函数()
  FROM 表名
     WHERE 行过滤条件 -------------分组前的过滤条件
      GROUP BY 分组字段------------分组内容
         HAVING  -----------------分组后过滤条件
     ORDER BY 排序字段;
```
执行顺序：
from  where  group by  having  select  order by(永远最后)


-------------------------------------------------------------------------
# 函数
是指已经封装好的程序，可以直接拿来使用。
函数特点： 有且只有一个返回值
1. 系统函数：系统已经封装好的 直接使用
2. 自定义函数： 根据自己的需求，自己创建
## 1.系统函数
(1).转换函数
(2).字符型函数
(3).数值型函数
(4).日期型函数
(5).聚合函数
(6).开窗函数
(7).其他函数
```
--   单行注释
/* 
多行注释
*/

[] 里的内容可以省略
```
### (1).转换函数



#### 转换字符函数
to_char（目标数值或日期，['格式']）
作用：是将目标数值或者日期按照格式转换成字符类型
返回 字符
1．将数值转换成字符
```select 123.45,to_char(123.45) from dual;```
如何区分数值和字符——数值是右对齐字符是左对齐
```select 123.45,'123.45' from dual;```
格式
0      当整数位无数字则强制显示成 0，当小数位无数字则以 0 补全
    当整数位精度不足则显示成###，当小数位精度不足则四舍五入
9      当整数位无数字则格式不生效当小数位无数字则以 0 补全
    当整数位精度不足则显示成###，当小数位精度不足则四舍五入
```
select 123.45,to_char(123.45,'000.00') from dual;
select123.45,to_char(123.45,'00000.00')from dual;
——当整数位无数字则强制显示成 0
select123.45,to_char(123.45,'00000.0000')from dual;
-—当小数位无数字则以 0 补全
select 123.45,to_char(123.45,'000.0'）from dual;
-当小数位精度不足则四舍五入
select 123.45,to_char(123.44999999999999,'000.0'）from dual;
select 123.45,to_char(123.45,'9999999999999999.9999'）from dual;
——当整数位无数字则格式不生效  格式占位
```
添加货币符号
L----本地货币
＄----美元
```
select to_char(123.45,'L000.0000 元'）from dual -----￥123.4500
select to_char(123.45,'000.0000L'）from dual---------123.4500￥
select to_char(123.45,'000.0L000'）from dual---------无效
select to_char(123.45,'000.0$000'）from dual--------- $123.4500
select to_char(123.45,'000.0000$'）from dual--------- $123.4500
```
添加千位符 1，000
```
select to_char(123456789.123456,'999,999,999.99999999')from dual
select to_char(123456789.123456,'999,999,999.99999999')from dual
```
```
selectto_char(sysdate,'d'）本周第几天 from dual;
select to_char(sysdate,'dd'）本月第几天 from dual;
selectto_char(sysdate,'ddd')本年第几天 from dual;
selectto_char(sysdate,'w'）本月第几天 fromdual;
selectto_char(sysdate,'ww'）本年第几天 fromdual;
select to_char(sysdate,'w'）本月第几周from dual;
selectto_char(sysdate,'ww'）本年第几周 from dual;
select to_char(sysdate,'mm'）本年第几月from dual;
selectto_char(sysdate,'q'）本年第几季度fromdual;
```

### (2).字符型函数
  大多数处理字符
  
#### 1.连接函数

#### 2.大小写转换函数
**(1)大写函数**
upper(c1)
【功能】将字符串全部转为大写
【参数】c1，字符表达式
【返回】字符型
【示例】select upper('AaBbCcDd') upper from dual; ----AABBCCDD
**(2)小写函数**
lower(字符)
**(3)首字母大写函数**
initcap(字符)
#### ==3.获取位置函数==

#### ==3.获取字符长度函数==
